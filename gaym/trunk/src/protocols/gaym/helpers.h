#include <string.h>
#include <glib.h>
char * return_string_between(const char* startbit, const char* endbit, const char* source);

char* convert_nick_to_gc(char*);
char* convert_nick_from_gc(char*);
